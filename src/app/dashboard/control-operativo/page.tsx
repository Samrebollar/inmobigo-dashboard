import { ControlOperativoClient } from '@/components/dashboard/control-operativo-client'

export const metadata = {
    title: 'Control Operativo | InmobiGo',
    description: 'Gestión operativa de tareas, proveedores y seguimiento de actividades del condominio.',
}

export default function ControlOperativoPage() {
    return <ControlOperativoClient />
}
